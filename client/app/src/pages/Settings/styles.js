import styled from 'styled-components';
import { colors } from '../../shared/constants';

const { white, grayLight } = colors;

export const Wrapper = styled.div`
  padding: 5% 15%;
  background: ${white};
`;

export const Container = styled.div`

`;
